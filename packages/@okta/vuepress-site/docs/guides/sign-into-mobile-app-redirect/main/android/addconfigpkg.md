Add the latest [Okta OIDC library](https://github.com/okta/okta-oidc-android) to the `dependencies` section of the `app/build.gradle` file:

```gradle
implementation 'com.okta.android:okta-oidc-android:1.2.2'
```
