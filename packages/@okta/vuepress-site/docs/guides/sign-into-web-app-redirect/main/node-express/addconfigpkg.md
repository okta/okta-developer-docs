Use `express-session`, `passport`, and `passport-openidconnect` in your Node Express application. Install the latest version of the npm packages with the following command:

```bash
npm install express-session passport passport-openidconnect
```
