1. Create a project folder named `okta-flask-quickstart` with a subfolder called `venv`.
1. Activate the corresponding environment using the following commands:

   ```bash
   cd okta-flask-quickstart
   python -m venv venv
   . venv/bin/activate
   ```
