If ngrok is already installed, skip to [Set up for profile enrollment (SSR scenario)](#set-up-for-profile-enrollment-ssr-scenario). If not, install it from the [ngrok download page](https://ngrok.com/download) in your sample app folder or the location of your choice.

>**Note:** You can install ngrok in the system path directory or the project directory. Alternatively, you can install the executable in another local directory, as long as you reference the directory path when using the tool.
