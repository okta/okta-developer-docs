
```json
// if ( data.context.conflicts == ["login"] ) {
// newLoginValue = addUniqueness ( data.user.profile.login );
// }
// 
// Working idea for adding uniqueness: insert middle name, which would have to 
// be supplied as a field in the csv file.
// george.smith@example.com would become george.alexander.smith@example.com.
//
```
